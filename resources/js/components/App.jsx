import React from "react";

const App = (props) => {
    // return console.log(props);
    return (
        <div>
            <h1>Laravel react {props.test}</h1>
        </div>
    );
};
export default App;
